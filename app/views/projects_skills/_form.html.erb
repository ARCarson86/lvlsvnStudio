<%= form_for(@projects_skill) do |f| %>
  <% if @projects_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projects_skill.errors.count, "error") %> prohibited this projects_skill from being saved:</h2>

      <ul>
      <% @projects_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
