<%= form_for(@projects_testimonial) do |f| %>
  <% if @projects_testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projects_testimonial.errors.count, "error") %> prohibited this projects_testimonial from being saved:</h2>

      <ul>
      <% @projects_testimonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
