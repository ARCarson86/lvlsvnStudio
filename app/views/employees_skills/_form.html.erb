<%= form_for(@employees_skill) do |f| %>
  <% if @employees_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employees_skill.errors.count, "error") %> prohibited this employees_skill from being saved:</h2>

      <ul>
      <% @employees_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
