<%= form_for(@employees_project) do |f| %>
  <% if @employees_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employees_project.errors.count, "error") %> prohibited this employees_project from being saved:</h2>

      <ul>
      <% @employees_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
